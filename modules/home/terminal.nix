{
  programs = {
    # TODO: try ghostty: https://ghostty.org/
    kitty = {
      enable = true;
      settings = {
        confirm_os_window_close = 0;
      };
    };

    zellij = {
      enable = true;
    };

    btop = {
      enable = true;
    };
  };
}
